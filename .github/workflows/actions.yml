name: CI

on:
  push:
    branches:
      - actions

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Start Docker container
        run: docker run -d my-docker-container

      - name: Verify Docker container
        run: docker ps -a | grep my-docker-container

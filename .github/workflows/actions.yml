name: CI

on:
  push:
    branches:
      - actions

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Start Docker container
        run: docker compose up

      - name: Verify Docker container
        run: docker ps -a | grep my-docker-container
